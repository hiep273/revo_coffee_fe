# Integrations

Currently, the project is a frontend single-page application with mock/static data.

## APIs
- No external backend API integrations are explicitly configured in the `package.json` or `vite.config.js`. Data appears to be served from `src/data/products.js` or managed locally.

## Third-Party Services
- **i18next**: For localization (English and Vietnamese).
- No payment gateways or authentication providers are currently integrated.
