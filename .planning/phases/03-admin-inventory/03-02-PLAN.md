---
phase: 03-admin-inventory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - be_revo_coffee/be_revo_coffee/Controllers/BatchesController.cs
  - be_revo_coffee/be_revo_coffee/Controllers/InventoryController.cs
  - revo-coffee-react/src/pages/admin/Inventory.jsx
  - revo-coffee-react/src/pages/admin/Batches.jsx
autonomous: true
requirements:
  - REQ-05
must_haves:
  truths:
    - "Admins can view and update inventory levels for products"
    - "Admins can create and manage production batches linked to products"
  artifacts:
    - path: "be_revo_coffee/be_revo_coffee/Controllers/InventoryController.cs"
      provides: "Inventory read/update API"
    - path: "be_revo_coffee/be_revo_coffee/Controllers/BatchesController.cs"
      provides: "Batch create/list API"
  key_links:
    - from: "revo-coffee-react/src/pages/admin/Inventory.jsx"
      to: "/api/admin/inventory"
      via: "fetch/POST"
---

<objective>
Add inventory and batch management features for admins. Provide backend endpoints and simple admin UI pages to list and mutate inventory and batches.
Purpose: maintain accurate stock levels and batch tracking for inventory operations.
Output: `InventoryController`, `BatchesController`, `Inventory.jsx`, `Batches.jsx`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement InventoryController (backend)</name>
  <files>be_revo_coffee/be_revo_coffee/Controllers/InventoryController.cs</files>
  <action>Add `/api/admin/inventory` with `GET` to list stock levels and `PUT /{productId}` to update quantity. Return sample data if DB absent.
  <verify>
    <automated>curl.exe http://localhost:5083/api/admin/inventory || true</automated>
  </verify>
  <done>Inventory endpoints return JSON and accept updates.</done>
</task>

<task type="auto">
  <name>Task 2: Implement BatchesController and UI</name>
  <files>be_revo_coffee/be_revo_coffee/Controllers/BatchesController.cs, revo-coffee-react/src/pages/admin/Batches.jsx</files>
  <action>Create `/api/admin/batches` endpoints (`GET`, `POST`) and a `Batches.jsx` admin page to list/add batches connected to products. Use sample payloads for now.
  <verify>
    <automated>grep -R "batches" be_revo_coffee || true</automated>
  </verify>
  <done>Batches API returns sample list and accepts new batch creation; admin page calls endpoints successfully.</done>
</task>

</tasks>

<verification>
Run backend and open admin Inventory/Batches pages or hit endpoints with `curl` to verify read/update flows.
</verification>

<success_criteria>
- `GET /api/admin/inventory` and `GET /api/admin/batches` return JSON.
- `PUT /api/admin/inventory/{productId}` updates stock (sample behavior acceptable).
- Admin pages render lists and allow create/update operations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-inventory/03-02-SUMMARY.md`.
</output>
