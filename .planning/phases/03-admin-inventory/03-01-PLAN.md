---
phase: 03-admin-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - be_revo_coffee/be_revo_coffee/Controllers/AdminProductsController.cs
  - revo-coffee-react/src/pages/admin/Products.jsx
  - revo-coffee-react/src/store/useStore.js
autonomous: true
requirements:
  - REQ-05
must_haves:
  truths:
    - "Admin users can create, update, list, and delete products via API"
    - "Admin UI exposes a products table with edit/create/delete actions"
  artifacts:
    - path: "be_revo_coffee/be_revo_coffee/Controllers/AdminProductsController.cs"
      provides: "Admin product CRUD API"
    - path: "revo-coffee-react/src/pages/admin/Products.jsx"
      provides: "Admin products management UI"
  key_links:
    - from: "revo-coffee-react/src/pages/admin/Products.jsx"
      to: "/api/admin/products"
      via: "fetch / axios calls"
---

<objective>
Implement admin product management: backend CRUD API and a simple admin UI to manage products.
Purpose: enable content management and inventory control for product catalog.
Output: `AdminProductsController` with CRUD routes and `Products.jsx` admin page wired to those endpoints.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AdminProductsController (backend)</name>
  <files>be_revo_coffee/be_revo_coffee/Controllers/AdminProductsController.cs</files>
  <action>Create `AdminProductsController` with attribute routing under `/api/admin/products`. Implement `GET`, `POST`, `PUT {id}`, `DELETE {id}`. For now return in-memory/sample JSON and simple validation. Keep methods idempotent and JSON-only. Document places to replace with real DB access.
  <verify>
    <automated>curl.exe http://localhost:5083/api/admin/products || true</automated>
  </verify>
  <done>Controller responds to GET/POST/PUT/DELETE with 200/201 where appropriate and JSON payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Add Admin Products UI (frontend)</name>
  <files>revo-coffee-react/src/pages/admin/Products.jsx, revo-coffee-react/src/store/useStore.js</files>
  <action>Create `Products.jsx` in the admin pages to fetch `/api/admin/products`, render a simple table, and provide create/edit/delete flows (modals or inline). Use `useStore` for shared state; fall back to local component state if needed. Keep UI minimal and functional (no styling required).
  <verify>
    <automated>grep -R "admin/products" revo-coffee-react/src || true</automated>
  </verify>
  <done>Admin page fetches products from `/api/admin/products`, displays rows, and triggers create/update/delete calls.</done>
</task>

</tasks>

<verification>
Start backend and open the admin page (or curl API) to validate product CRUD operations. If backend returns sample data, frontend should render it and POST/PUT/DELETE should return success statuses.
</verification>

<success_criteria>
- `GET /api/admin/products` returns JSON list.
- Admin UI lists products and create/edit/delete actions return successful responses.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-inventory/03-01-SUMMARY.md`.
</output>
