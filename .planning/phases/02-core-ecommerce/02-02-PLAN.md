---
phase: 02-core-ecommerce
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - revo-coffee-react/src/pages/Login.jsx
  - revo-coffee-react/src/pages/Register.jsx
  - revo-coffee-react/src/pages/Orders.jsx
  - be_revo_coffee/Controllers/AuthController.cs
  - be_revo_coffee/Controllers/OrdersController.cs
autonomous: true
requirements:
  - REQ-03
  - REQ-04
must_haves:
  truths:
    - "Users can register and log in to create a session"
    - "Authenticated users can create orders and view their order history"
  artifacts:
    - path: "revo-coffee-react/src/pages/Login.jsx"
      provides: "Login UI and form submit"
    - path: "be_revo_coffee/Controllers/OrdersController.cs"
      provides: "Order creation and list endpoints"
  key_links:
    - from: "revo-coffee-react/src/pages/Checkout.jsx"
      to: "/api/orders"
      via: "POST order payload"
---

<objective>
Implement basic user authentication UI and order creation/history endpoints so purchases can be created and viewed by authenticated users.
Purpose: enable end-to-end ordering flows (without external payments in this phase).
Output: login/register pages, order pages, backend endpoints for auth and orders (sample implementation acceptable).
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement login/register UI</name>
  <files>revo-coffee-react/src/pages/Login.jsx, revo-coffee-react/src/pages/Register.jsx</files>
  <action>Create simple forms for register and login that POST to `/api/auth/register` and `/api/auth/login`. On success, store a session token in `httpOnly` cookie via backend or in `useStore` for local dev flows.</action>
  <verify>
    <automated>grep -R "POST.*auth" revo-coffee-react/src || true</automated>
  </verify>
  <done>Login/Register pages submit credentials to backend endpoints and handle success/error states.</done>
</task>

<task type="auto">
  <name>Task 2: Add order endpoints and order-history UI</name>
  <files>be_revo_coffee/Controllers/OrdersController.cs, revo-coffee-react/src/pages/Orders.jsx</files>
  <action>Implement `POST /api/orders` to create orders (accept cart payload) and `GET /api/orders` to list user orders. Implement `Orders.jsx` to fetch and render user's orders when authenticated. Use sample data if DB not ready.</action>
  <verify>
    <automated>grep -i "orders" be_revo_coffee/Controllers || grep -R "Orders.jsx" revo-coffee-react/src || true</automated>
  </verify>
  <done>User can create an order (via POST) and view order history in the UI (GET).</done>
</task>

</tasks>

<verification>
Run the frontend and backend locally or via Docker; register/login a user and create an order through checkout to validate the flow.
</verification>

<success_criteria>
- `POST /api/auth/login` and `POST /api/auth/register` exist and return success states.
- `POST /api/orders` accepts an order payload and `GET /api/orders` returns orders for the authenticated user.
- Orders page lists any created orders for the user.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ecommerce/02-02-SUMMARY.md`.
</output>
