---
phase: 02-core-ecommerce
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - revo-coffee-react/src/components/Products.jsx
  - revo-coffee-react/src/pages/ProductDetail.jsx
  - revo-coffee-react/src/pages/Shop.jsx
  - revo-coffee-react/src/pages/Cart.jsx
  - revo-coffee-react/src/data/products.js
  - be_revo_coffee/Controllers/productcontroler.cs
autonomous: true
requirements:
  - REQ-01
  - REQ-02
must_haves:
  truths:
    - "Users can view a product listing with search and basic filters"
    - "Users can open a product detail page showing product information"
    - "Users can add items to a cart and see them reflected in the cart UI"
  artifacts:
    - path: "revo-coffee-react/src/components/Products.jsx"
      provides: "Product list rendering"
    - path: "revo-coffee-react/src/pages/ProductDetail.jsx"
      provides: "Product detail view"
    - path: "be_revo_coffee/Controllers/productcontroler.cs"
      provides: "Product API endpoints (GET /api/products, GET /api/products/{id})"
  key_links:
    - from: "revo-coffee-react/src/pages/Shop.jsx"
      to: "/api/products"
      via: "fetch in useEffect"
---

<objective>
Implement product catalog UI and API wiring so users can browse products and add them to a shopping cart.
Purpose: deliver the core browsing experience required for e-commerce flows.
Output: frontend components for listing and detail, data module for products, backend product endpoints.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add/Improve product list and detail components</name>
  <files>revo-coffee-react/src/components/Products.jsx, revo-coffee-react/src/pages/ProductDetail.jsx, revo-coffee-react/src/pages/Shop.jsx</files>
  <action>Create or update the product listing (`Products.jsx`) to fetch products from `/api/products` (or read `src/data/products.js` when API is unavailable). Implement `ProductDetail.jsx` to display product fields and an Add to Cart button. Ensure cart state is stored in `useStore` or localStorage for persistence.</action>
  <verify>
    <automated>grep -R "export default" revo-coffee-react/src/components/Products.jsx || true; grep -R "ProductDetail" revo-coffee-react/src/pages || true</automated>
  </verify>
  <done>Product listing renders with product items; product detail page shows product details and adds item to cart.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure backend product endpoints exist</name>
  <files>be_revo_coffee/Controllers/productcontroler.cs</files>
  <action>Verify or implement simple GET endpoints in `productcontroler.cs`: `GET /api/products` and `GET /api/products/{id}` returning JSON product lists/objects. If full DB wiring is not ready, return static sample data for now.</action>
  <verify>
    <automated>grep -i "GET" be_revo_coffee/Controllers/productcontroler.cs || grep -i "products" be_revo_coffee/Controllers/productcontroler.cs || true</automated>
  </verify>
  <done>Backend exposes `GET /api/products` and `GET /api/products/{id}` returning JSON (can be sample data).</done>
</task>

</tasks>

<verification>
Run frontend locally or through Docker and hit `/api/products` to validate list rendering. If API returns sample data, frontend should render items from it.
</verification>

<success_criteria>
- Product list page displays product cards pulled from API or local data.
- Product detail page displays product info and Add to Cart functions.
- `GET /api/products` responds with JSON when backend is running.
</success_criteria>

<output>
After completion, publish `.planning/phases/02-core-ecommerce/02-01-SUMMARY.md`.
</output>
