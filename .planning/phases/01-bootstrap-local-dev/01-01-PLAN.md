---
phase: 01-bootstrap-local-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - README.docker.md
  - revo-coffee-react/package.json
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
must_haves:
  truths:
    - "Developers can start both frontend and backend locally with a single command"
    - "Docker Compose defines both services and builds images successfully"
    - "Developer README documents dev-start steps and common commands"
  artifacts:
    - path: "docker-compose.yml"
      provides: "local multi-service compose for frontend+backend"
    - path: "README.docker.md"
      provides: "developer startup instructions"
  key_links:
    - from: "revo-coffee-react"
      to: "be_revo_coffee"
      via: "docker-compose service definitions"
---

<objective>
Bring local development up-to-date so a developer can run both frontend and backend with Docker Compose and have clear startup docs.
Purpose: reduce onboarding friction and ensure consistent local environment.
Output: updated `docker-compose.yml`, `README.docker.md` with dev-start steps, verification commands.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve README and dev-start docs</name>
  <files>README.docker.md</files>
  <action>Update `README.docker.md` with explicit dev-start commands for Windows and Unix, include `docker-compose up --build`, frontend dev command `cd revo-coffee-react && npm install && npm run dev`, and common troubleshooting steps.</action>
  <verify>
    <automated>grep -n "docker-compose up" README.docker.md || true</automated>
  </verify>
  <done>README.docker.md contains `docker-compose up --build` and a section titled "Developer Quick Start".</done>
</task>

<task type="auto">
  <name>Task 2: Validate Docker Compose starts both services</name>
  <files>docker-compose.yml</files>
  <action>Ensure `docker-compose.yml` defines two services: `frontend` (builds `revo-coffee-react`, exposes dev port) and `backend` (builds `be_revo_coffee`, exposes API port). Add `depends_on` where appropriate and healthcheck commands if possible.</action>
  <verify>
    <automated>docker-compose -f docker-compose.yml config && docker-compose -f docker-compose.yml up --build -d && docker ps --filter "name=revo-coffee-react" --filter "name=be_revo_coffee"</automated>
  </verify>
  <done>`docker-compose config` succeeds and both services appear in `docker ps` when started.</done>
</task>

</tasks>

<verification>
Run the automated verify commands in order. If Docker is unavailable, mark verification as manual and follow the steps in `README.docker.md`.
</verification>

<success_criteria>
- `docker-compose -f docker-compose.yml config` returns valid output.
- `README.docker.md` includes copyable quick-start commands for Windows and Unix.
- Both frontend and backend services can be started locally using the documented commands.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-local-dev/01-01-SUMMARY.md` after execution.
</output>
